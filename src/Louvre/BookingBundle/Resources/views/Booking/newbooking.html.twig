{% extends "@Booking/layout.html.twig" %}

{% block head %}
    {{ parent() }}
    {%  block title %}Billeterie{% endblock %}
{% endblock %}

{% block body %}

<div class="row">
    <div class="container">
        <div class="row col-md-6 col-md-offset-3" id="first">

                {{ form_start(form, {'attr': {'novalidate': 'novalidate', "class": "group"}}) }}
                    <div class="form-group">
                        {{ form_errors(form.dateOfVisit) }}
                        {{ form_label(form.dateOfVisit) }}
                        <br>
                        {{ form_widget(form.dateOfVisit) }}
                    </div>
                    <div class="form-group">
                        {{ form_errors(form.durationOfVisit) }}
                        {{ form_label(form.durationOfVisit) }}
                        <br>
                        {{ form_widget(form.durationOfVisit) }}
                    </div>
                    <div class="form-group">
                        {{ form_errors(form.numberOfTickets) }}
                        {{ form_label(form.numberOfTickets) }}
                        <br>
                        {{ form_widget(form.numberOfTickets) }}
                    </div>
                    <div class="form-group">
                        {{ form_errors(form.clientEmail) }}
                        {{ form_label(form.clientEmail) }}
                        <br>
                        {{ form_widget(form.clientEmail) }}
                    </div>
                    <button class="btn btn-primary btn-block">Réserver</button>
                {{ form_end(form) }}

        </div>
    </div>
</div>

{% endblock %}