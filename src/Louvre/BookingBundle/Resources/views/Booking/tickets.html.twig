{% extends "@Booking/layout.html.twig" %}

{% block head %}
    {{ parent() }}
    {%  block title %}Billeterie - Visiteurs{% endblock %}
{% endblock %}

{% block body%}

    <div class="container ">

        <div class="row col-md-6 col-md-offset-3" id="second">

            {{ form(form, {attr: {novalidate: "novalidate", "class": "{#form-inline#} group"}}) }}
            {#{{ form_start(form, {'attr': {'novalidate': 'novalidate', "class": "group #}{#form-inline#}{#"}}) }}
            {{ form_end(form) }}#}
            <a href="{{ path('newbooking', {"id": reservation.id}) }}"><button class="btn btn-info btn-block" id="retourPageOne">Revenir à l'étape précédente</button></a>
            <br>

        </div>

    </div>

{% endblock %}